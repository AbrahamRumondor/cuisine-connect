package com.example.cuisineconnect.app.screen.recipe.detail.viewHolder

import androidx.recyclerview.widget.RecyclerView
import com.example.cuisineconnect.databinding.ViewBottomSpaceBinding

class ViewBottomSpacingViewHolder(
    private val view: ViewBottomSpaceBinding,
) : RecyclerView.ViewHolder(view.root) {

    fun bind() {
    }
}